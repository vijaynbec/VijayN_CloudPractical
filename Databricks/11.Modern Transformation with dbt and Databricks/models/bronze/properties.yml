### model level tests
# models: 
  # - name: bronze_products
  #   columns:
  #     - name: category
  #       tests:
  #         - accepted_values:
  #           values: "Electronics","Wearables","Stationery","Home"]
version: 2
models:
  - name: bronze_products
    columns:
      - name: category
        data_tests:
          - accepted_values:
              arguments: # available in v1.10.5 and higher. Older versions can set the <argument_name> as the top-level property.
                values: ["Electronics","Wearables","Stationery","Home"]
      # - name: id
      #   data_tests:
      #     - relationships:
      #         arguments: # available in v1.10.5 and higher. Older versions can set the <argument_name> as the top-level property.
      #           to: ref('bronze_orders')
      #           field: product_id


